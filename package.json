{
  "name": "foundry-lpc-mobile",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "check": "node tests/check.mjs",
    "format": "prettier --write scripts styles tests playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "prettier": "^3.6.2"
  }
}
